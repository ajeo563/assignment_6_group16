# CodeCraft - Learning Progress API

A Spring Boot REST API implementing the View Learning Progress feature for the CodeCraft OOP learning platform.

## Feature
Returns a summary of a user's learning progress including modules completed, in progress, and not started, quiz scores, and total time spent.

## Running the Application
```bash
mvnw.cmd spring-boot:run
```

## Endpoint